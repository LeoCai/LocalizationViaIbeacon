package com.leocai.beaconlocalization.localization.AlgorithmLocalization;

import com.leocai.beaconlocalization.localization.LocalizationInfo;

import java.util.List;

/**
 * 根据三个beacon位置
 * Created by leocai on 15-4-2.
 */
public class AlgorithmThreeNeighbours {


    private double[] getPositionByThreeNeighbours(List<LocalizationInfo> sortedLocalizationInfos) {
        if (sortedLocalizationInfos.size() < 3) return null;
        LocalizationInfo info0 = sortedLocalizationInfos.get(0);
        LocalizationInfo info1 = sortedLocalizationInfos.get(1);
        LocalizationInfo info2 = sortedLocalizationInfos.get(2);
        return null;
//        return new double[]{(basePos0[0] + basePos1[0] + basePos2[0]) / 3,
//                (basePos0[1] + basePos1[1] + basePos2[1]) / 3};
    }
}
